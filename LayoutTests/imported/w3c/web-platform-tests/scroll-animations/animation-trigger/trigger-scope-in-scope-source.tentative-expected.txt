In-scope Target
SOURCE
Out-of-scope Target

FAIL In-scope target finds trigger of source; out-of-scope target does not. assert_equals: animation on in_scope_target has playState running. expected "running" but got "paused"

